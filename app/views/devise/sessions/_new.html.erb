<div style="background-color: #666; padding-left: 2%; color: #eee">
    <h2 style="font-size: 1.8em  ">Entrar</h2>
</div>
<div style="background-color: #fff; padding: 2%">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "loginForm") do |f| %>
    <div class="form-group" style="font-size: 1.3em">
      <%= f.label :correo %>
      <%= f.email_field :email, autofocus: true, :class => 'form-control' %>
    </div>

    <div class="field" style="font-size: 1.3em">
      <%= f.label :Contraseña %>
      <%= f.password_field :password, autocomplete: "off", :class => 'form-control' %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end -%>

    <div class="actions">
      <%= f.submit "Acceder", :class => 'btn btn-block' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>

  </div>

