<% console 	 %>
<main class="detalle" >
	<div class="breadcrumb-class">
    	Está en:<a href="#" target="_self" title="Insertar numero de documento">Insertar numero de documento</a>
    </div>
	<div class="container-fluid" >
		<%=	 form_for @student, url: create_student_url do |f| %>

		  <%= f.hidden_field :name, value: @student.name %>
		  <%= f.hidden_field :lastname, value: @student.lastname %>
		  <%= f.hidden_field :email, value: @student.email %>
		  <%= f.hidden_field :rol, value: @student.rol %>
		  <%= f.hidden_field :name, value: @student.name %>
		  <div class="row">
		  
		  	<div class="panel panel-info col-md-3 col-md-offset-4">
		  		<div class="panel-heading">
   					<h3 class="panel-title">Ingrese Documento</h3>
  				</div>
  				<div class="panel-body">
    				<div class="field">
		    			<%= f.number_field :identification, class: "form-control"%>
		  			</div>
  				</div>
  				<div class="panel-footer">
  					<%= f.hidden_field :faculty, value: @student.faculty %>
					<%= f.hidden_field :career, value: @student.career %>
					<%= f.submit "Guardar", class: "btn btn-success"%>
  				</div>
			</div>
		   </div>
		  
		<% end %>
	</div>
</main>